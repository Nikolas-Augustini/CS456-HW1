using UnityEngine;

public class ColorChange : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        objectRenderer = GetComponent<Render
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
